{
  "name": "ECDSA nonce reuse private key recovery",
  "category": "CRYPTOGRAPHY",
  "difficulty": "HARD",
  "topic": "ECDSA nonce reuse private key recovery",
  "points": 400,
  "estimated_time": 70,
  "flag": "pwn{ecdsa_nonce_reuse_private_key_re_e3ad17}",
  "storage_path": "crypto/hard-ecdsa-nonce",
  "primary_artifact": "ciphertext.bin",
  "description": "ECDSA nonce reuse private key recovery -- a hard-tier cryptography challenge. Review the bundled artifacts, locate the encoded clue, decode it, and recover the flag. Flag format: pwn{...}",
  "objectives": [
    "Inspect the challenge bundle for \"ECDSA nonce reuse private key recovery\"",
    "Locate the encoded artifact (look in every shipped file)",
    "Unwrap the encoding applied to the artifact",
    "Recover the plaintext flag and submit it in pwn{...} format"
  ],
  "hints": [
    {
      "text": "Start by inspecting every file in the bundle (hex dump, headers, magic bytes).",
      "penalty": 5
    },
    {
      "text": "The artifact is wrapped in a non-text container -- use the right parser.",
      "penalty": 10
    },
    {
      "text": "The artifact is a double layer: hex -> base64 -> XOR. Unwind both layers and recover the 4-byte XOR key.",
      "penalty": 15
    }
  ]
}
