{
  "name": "Heap exploitation: fastbin dup and tcache poisoning leading to arbitrary write",
  "category": "BINARY EXPLOITATION",
  "difficulty": "HARD",
  "topic": "Heap exploitation: fastbin dup and tcache poisoning leading to arbitrary write",
  "points": 400,
  "estimated_time": 60,
  "flag": "pwn{heap_exploitation_fastbin_dup_an_50f116}",
  "storage_path": "pwn/hard-tcache-poison",
  "primary_artifact": "vuln_service.elf",
  "description": "Heap exploitation: fastbin dup and tcache poisoning leading to arbitrary write -- a hard-tier binary exploitation challenge. Review the bundled artifacts, locate the encoded clue, decode it, and recover the flag. Flag format: pwn{...}",
  "objectives": [
    "Inspect the challenge bundle for \"Heap exploitation: fastbin dup and tcache poisoning leading to arbitrary write\"",
    "Locate the encoded artifact (look in every shipped file)",
    "Unwrap the encoding applied to the artifact",
    "Recover the plaintext flag and submit it in pwn{...} format"
  ],
  "hints": [
    {
      "text": "Start by inspecting every file in the bundle (hex dump, headers, magic bytes).",
      "penalty": 5
    },
    {
      "text": "The artifact is wrapped in a non-text container -- use the right parser.",
      "penalty": 10
    },
    {
      "text": "The artifact is a double layer: hex -> base64 -> XOR. Unwind both layers and recover the 4-byte XOR key.",
      "penalty": 15
    }
  ]
}
