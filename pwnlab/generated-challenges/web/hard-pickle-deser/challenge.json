{
  "name": "Python pickle insecure deserialization RCE via session cookie",
  "category": "WEB",
  "difficulty": "HARD",
  "topic": "Python pickle insecure deserialization RCE via session cookie",
  "points": 400,
  "estimated_time": 60,
  "flag": "pwn{python_pickle_insecure_deseriali_7efb3c}",
  "storage_path": "web/hard-pickle-deser",
  "primary_artifact": "response.json",
  "description": "Python pickle insecure deserialization RCE via session cookie -- a hard-tier web challenge. Review the bundled artifacts, locate the encoded clue, decode it, and recover the flag. Flag format: pwn{...}",
  "objectives": [
    "Inspect the challenge bundle for \"Python pickle insecure deserialization RCE via session cookie\"",
    "Locate the encoded artifact (look in every shipped file)",
    "Unwrap the encoding applied to the artifact",
    "Recover the plaintext flag and submit it in pwn{...} format"
  ],
  "hints": [
    {
      "text": "Start by inspecting every file in the bundle (hex dump, headers, magic bytes).",
      "penalty": 5
    },
    {
      "text": "The artifact is wrapped in a non-text container -- use the right parser.",
      "penalty": 10
    },
    {
      "text": "The artifact is a double layer: hex -> base64 -> XOR. Unwind both layers and recover the 4-byte XOR key.",
      "penalty": 15
    }
  ]
}
