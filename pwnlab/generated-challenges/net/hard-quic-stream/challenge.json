{
  "name": "QUIC protocol stream multiplexing covert channel",
  "category": "NETWORKING",
  "difficulty": "HARD",
  "topic": "QUIC protocol stream multiplexing covert channel",
  "points": 400,
  "estimated_time": 70,
  "flag": "pwn{quic_protocol_stream_multiplexin_4ed126}",
  "storage_path": "net/hard-quic-stream",
  "primary_artifact": "capture.pcap",
  "description": "QUIC protocol stream multiplexing covert channel -- a hard-tier networking challenge. Review the bundled artifacts, locate the encoded clue, decode it, and recover the flag. Flag format: pwn{...}",
  "objectives": [
    "Inspect the challenge bundle for \"QUIC protocol stream multiplexing covert channel\"",
    "Locate the encoded artifact (look in every shipped file)",
    "Unwrap the encoding applied to the artifact",
    "Recover the plaintext flag and submit it in pwn{...} format"
  ],
  "hints": [
    {
      "text": "Start by inspecting every file in the bundle (hex dump, headers, magic bytes).",
      "penalty": 5
    },
    {
      "text": "The artifact is wrapped in a non-text container -- use the right parser.",
      "penalty": 10
    },
    {
      "text": "The artifact is a double layer: hex -> base64 -> XOR. Unwind both layers and recover the 4-byte XOR key.",
      "penalty": 15
    }
  ]
}
